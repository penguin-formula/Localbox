FROM loxclient-base

# Make translations
RUN make translations

# User stuff
RUN useradd -ms /bin/bash containeruser
USER containeruser

RUN mkdir -p /home/containeruser/.config/localbox
RUN mkdir -p /home/containeruser/.local/share

# Container will start this command
ENTRYPOINT ["python", "-m", "sync"]
